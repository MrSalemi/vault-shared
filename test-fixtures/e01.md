---
out: E01.docx
version: V01
title: "CI fixture guide"
number: "01"
scaffold: fixture.py
---

This is a synthetic guide used only to exercise the builder in CI. It is
not real course content — see [[e02|CI fixture guide two]] for the paired
sibling used by the link-handling checks.

An equation rides along here so the whole-guide checks cover math too: the
"builds twice identically" one in particular, since a Word equation is a
tree of objects and a determinism bug there would not show up in any of the
math tests, which each build only once. $a = \frac{v_f - v_i}{\Delta t}$

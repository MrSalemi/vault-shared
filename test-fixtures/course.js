// Synthetic course.js for CI only. Not a real course — supplies no real
// placeholder text, just enough for test-build.js to exercise the builder
// against something that looks like a guides folder.
module.exports = () => ({});
